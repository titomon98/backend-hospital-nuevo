# Secreto para firmar y verificar los JWT.
# Debe ser el mismo en login, refresh y el middleware de auth.
# Si se cambia, todos los tokens vigentes dejan de ser válidos y
# los usuarios tienen que volver a iniciar sesión.
JWT_SECRET=

# Puerto del servidor (opcional, por defecto 3000)
# PORT=3000
